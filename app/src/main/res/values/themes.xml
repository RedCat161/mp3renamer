<resources>
    <!-- Принудительно тёмная тема (не зависит от системной DayNight) -->
    <style name="Theme.Mp3Renamer" parent="Theme.MaterialComponents.NoActionBar">
        <item name="colorPrimary">@color/accent_primary</item>
        <item name="colorPrimaryVariant">@color/accent_primary_variant</item>
        <item name="colorPrimaryDark">@color/background_root</item>
        <item name="colorAccent">@color/accent_primary</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorOnPrimary">@color/accent_on_primary</item>

        <item name="android:windowBackground">@color/background_root</item>
        <item name="android:statusBarColor">@color/background_root</item>
        <item name="android:navigationBarColor">@color/background_root</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>
</resources>
